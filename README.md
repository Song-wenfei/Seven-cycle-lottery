# Seven Cycle

Seven Cycle is a small installable PWA for iPhone. Add seven choices, tap Play once per day, and the app keeps each choice from repeating inside the current seven-day cycle.

## Run locally

From this folder:

```powershell
python -m http.server 4177
```

Then open `http://localhost:4177`.

## Install on iPhone

Host the folder on any static web host, open the URL in Safari on the iPhone, tap Share, then choose Add to Home Screen. After the first load, the app works offline from the Home Screen.
